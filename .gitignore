node_modules/
dist/
coverage/
playwright-report/
test-results/
.DS_Store
*.local
logs
*.log

# real Garmin data snapshot (100 activities, MCP pull) — local-only,
# optional richer dataset for local dev. src/data/activities.json (the
# tracked path lib/data.js reads) is a small synthetic sample instead.
src/data/activities.local.json
